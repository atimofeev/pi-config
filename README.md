# pi-config

Pi coding agent configuration — sub-agents, extensions, skills, MCP servers, settings, memory.

## Installation

```bash
just install
```

`just install` creates symlinks:

- `~/.pi/agent → pi-agent/`
- `~/.agents/skills → skills/`

Existing targets backed up to `<target>.backup.<timestamp>`.
